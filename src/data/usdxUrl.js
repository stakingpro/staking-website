let basePrex ="/web/api";
let registUrl = `${basePrex}/register`;
let loginUrl = `${basePrex}/login`;
let whiteListUrl = `${basePrex}/save_usdx_white_list`;
let saveBasic_info = `${basePrex}/saveBasic_info`
let uploadImage = `${basePrex}/uploadImage`
let sendMail = `${basePrex}/sendMail`
let restPassword = `${basePrex}/restPassword`
let setPassword = `${basePrex}/setPassword`
export {
    registUrl,
    loginUrl,
    restPassword,
    whiteListUrl,
    saveBasic_info,
    uploadImage,
    sendMail,
    setPassword
}